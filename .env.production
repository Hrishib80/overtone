# The production deployment serves the website and API from one address.
# Leave these unset for that setup.
#
# If you deploy the website and API separately, use origins only (no /api suffix):
# VITE_API_URL=https://api.example.com
# VITE_WS_URL=wss://api.example.com/ws
#
# Reliable calls outside local networks need a TURN server:
# VITE_TURN_URL=turn:turn.example.com:3478
# VITE_TURN_USERNAME=turn-username
# VITE_TURN_CREDENTIAL=turn-password

# Mail is required in production: an address that never receives a link can
# never be verified, and the campus gate would admit nobody.
MAIL_PROVIDER=smtp
MAIL_FROM=Overtone <no-reply@your-domain.edu>
PUBLIC_WEB_URL=https://your-domain.example
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_USE_STARTTLS=true
SMTP_USE_TLS=false
